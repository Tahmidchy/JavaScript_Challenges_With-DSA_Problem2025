/*

TODO: Problem-8: You have an array [1,2,3,4,5], you write a function, using forEach to print each number to the console.

*/

// Solution:

const NumbersArray = [1, 2, 3, 4, 5];
NumbersArray.forEach(number => console.log(number));
// The forEach method executes a provided function once for each array element, effectively iterating through the NumbersArray and printing each number to the console.
// This solution uses the forEach operator to iterate over each number in the NumbersArray and logs it to the console. The original array remains unchanged.
// The output is printed directly to the console, showing each number from the array on a new line.
// The forEach method does not return a new array; it simply performs the action of logging each number to the console.
// This solution demonstrates how to use the forEach method to perform an action on each element of an array without creating a new array.
// The output will be: