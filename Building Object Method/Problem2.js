/*
 TODO: Problem-2: Create a variable named willAttend and its value will be 0, now convert this variable to boolean type and look at the console.
*/

// Problem-2: Convert a number to boolean 

const willAttend = 0;
const attendanceStatus = Boolean(willAttend);

console.log(attendanceStatus); // Output: false
console.log(typeof attendanceStatus); // Output: "boolean"