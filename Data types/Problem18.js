/*
TODO: Problem-18: To see how many runs were scored in the super over, declare a variable named super and set a value in that variable. Check if the program runs. If not, mention why it doesn't in the comment.
*/

//Solution:
/*

let super = 16;
console.log(super); 
*/

//We are can't using "super" keyword for variable name in JavaScript because it is a reserved keyword. So, the above code will not run. also we can't use any reserved word as variable name in JavaScript. i think "super" word can using another programming language as variable name.