/*
TODO: Problem-10: An empty object {} takes a property, is it a falsy value, check it by writing it as a condition inside the if statement.
*/

// Problem-10: An empty object {} takes a property, is it a falsy value, check it by writing it as a condition inside the if statement.

const obj = {};
if(obj) {
    console.log("Object is truthy");
} else {
    console.log("Object is falsy");
}